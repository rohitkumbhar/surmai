package jobs

import (
	"log"
)

func ImportDemoDataJob() {
	log.Println(".Hello from importDemoDataJob!")

	// clean all existing trips
	//trips, err := app.FindAllRecords("trips", nil)

	// delete all existing users except admin
	// Reset application settings, especially smtp
	// create a new user demo@surmai.app
	// import trip
}
